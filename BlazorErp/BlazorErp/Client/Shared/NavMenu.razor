<Styled>
    .navicon {
        font-size: 24px;
    }
    .my-logo {
        font-weight: 800;
        font-size: 24px;
        color: #6772e5;
        text-align: center;
        height: 64px;
        line-height: 64px;
    }
</Styled>

<div class="my-logo">BlazorERP</div>
<Menu DefaultSelectedKeys=@(new[]{"1"})
      DefaultOpenKeys=@(new[]{"sub1"})
      Mode=MenuMode.Inline>
    <MenuItem Key="1" class="nav-menu-item">
        <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
            <Icon Class="navicon text-primary" Type="home" /> Home
        </NavLink>
    </MenuItem>
    <MenuItem Key="2">
        <NavLink class="nav-link" href="korisnici">
            <Icon Class="navicon text-primary" Type="user" /> Korisnici
        </NavLink>
    </MenuItem>
    <MenuItem Key="3">
        <NavLink class="nav-link" href="uloge">
            <Icon Class="navicon text-primary" Type="user" /> Uloge
        </NavLink>
    </MenuItem>
    <SubMenu Key="sub1" Title=sub1Title>
        <MenuItem Key="5">Option 5</MenuItem>
        <MenuItem Key="6">Option 6</MenuItem>
    </SubMenu>
    <MenuItem Key="4">
        <NavLink class="nav-link" href="posavke">
            <Icon Class="navicon text-primary" Type="setting" /> Posavke
        </NavLink>
    </MenuItem>
    <MenuItem Key="7">
        <NavLink class="nav-link" href="counter">
            <Icon Class="navicon text-primary" Type="plus-circle" /> Counter
        </NavLink>
    </MenuItem>
    <MenuItem Key="8">
        <NavLink class="nav-link" href="fetchdata">
            <Icon Class="navicon text-primary" Type="unordered-list" /> Fetch data
        </NavLink>
    </MenuItem>
</Menu>

@code {RenderFragment sub1Title =
    @<span>
        <Icon Type="audit" Theme="outline"></Icon>
        <span>Šifarnici</span>
    </span>;

private bool collapseNavMenu = true;

private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

private void ToggleNavMenu()
{
 collapseNavMenu = !collapseNavMenu;
}
}
